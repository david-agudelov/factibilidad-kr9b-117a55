# Public Templates

This folder is reserved for public template files that must be served directly by Vite.

Use `src/templates` for React-side templates. Use this folder only when the browser needs direct file access through `/templates/...`.
